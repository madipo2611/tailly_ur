# Development only: the UI returns this code instead of sending an SMS.
SMS_DEV_CODE=000000
SIGNING_URL_BASE=http://localhost:8080
CORS_ALLOWED_ORIGINS=http://localhost:8080
DATABASE_URL=postgres://digital_notary:development-only@localhost:5432/digital_notary?sslmode=disable
KAFKA_BROKERS=localhost:9092
KAFKA_TOPIC=digital-notary.documents
RABBITMQ_URL=amqp://digital_notary:development-only@localhost:5672/
# Use these only after configuring an isolated object-storage gateway.
# OBJECT_STORAGE_URL=https://objects.internal.example
# OBJECT_STORAGE_TOKEN=injected-by-vault
# S3_ENDPOINT=https://s3.regru.cloud
# S3_REGION=ru-central1
# S3_BUCKET=tictic
# S3_ACCESS_KEY_ID=injected-by-vault
# S3_SECRET_ACCESS_KEY=injected-by-vault
# STORAGE_CLIENT_CERT=/vault/secrets/storage-client.crt
# STORAGE_CLIENT_KEY=/vault/secrets/storage-client.key
# STORAGE_CA_CERT=/vault/secrets/storage-ca.crt
# VAULT_ENV_FILE=/vault/secrets/app.env
# SMS_GATEWAY_URL=https://sms-gateway.internal.example/v1/messages
# SMS_GATEWAY_TOKEN=injected-by-vault
# BILLING_WEBHOOK_SECRET=injected-by-vault
# UKEP_WEBHOOK_SECRET=injected-by-vault
